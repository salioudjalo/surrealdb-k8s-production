apiVersion: v1
kind: Service
metadata:
  name: surrealdb
  namespace: surrealdb
spec:
  selector:
    app: surrealdb
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP